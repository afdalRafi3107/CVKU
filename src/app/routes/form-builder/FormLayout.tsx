import { Outlet } from "react-router";

function FormLayout() {
  return (
    <section>
      <p>Personal detail - expreriences - generate </p>
      <Outlet />
    </section>
  );
}

export default FormLayout;
